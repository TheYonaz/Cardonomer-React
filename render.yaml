services:
  # Frontend - React App
  - type: web
    name: cardonomer-frontend
    env: static
    buildCommand: cd react-app && npm install && npm run build
    staticPublishPath: ./react-app/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

